return {
  'stevearc/dressing.nvim',
  opts = {
    input = {
      default_prompt = '>',
      relative = 'editor',
      prefer_width = 50,
      prompt_aling = 'center',
      win_options = { winblend = 0 }
    }
  },
}
